# tracksnap

Stateful URL and feed change tracker — remembers what it's seen, shows only what's new.

## Install

```
pip install tracksnap
```

## Usage

```
tracksnap [TARGET] --limit 10 --output json
```

Formats: text, json, table, csv. Run `tracksnap introspect` for a machine-readable
description.
